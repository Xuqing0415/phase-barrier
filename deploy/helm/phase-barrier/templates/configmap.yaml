{{- if .Values.sidecar.configYaml }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "phase-barrier.fullname" . }}-config
  labels:
    {{- include "phase-barrier.labels" . | nindent 4 }}
data:
  gate.yaml: |
{{ .Values.sidecar.configYaml | indent 4 }}
{{- end }}
